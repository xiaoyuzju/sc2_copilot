# Bundle versioned map schedule snapshots

SC2 Copilot 只在开发阶段按固定流程抓取灰机 Wiki，保存带来源地址和采集时间的原始快照，经规范化和 Keiframe 差异检查后，将规范化 JSON 在构建时嵌入 Rust 可执行文件。原始网页快照和差异报告只保留在仓库中，不进入运行时数据；运行中的应用不加载外部时间表，不访问灰机 Wiki，也不静默更新地图时间表。这样牺牲了网站更新后的即时同步和脱离构建单独替换数据的能力，换取数据与程序版本严格对应、离线可用、行为确定、发布内容可审查和历史结果可复现；重复更新步骤由项目运行手册固定。
