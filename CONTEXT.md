# SC2 Copilot

SC2 Copilot 是《星际争霸 II》合作模式的观察型辅助软件。它在游戏进程外理解当前对局，并向玩家提供及时、只读的辅助信息。

## Language

**观察型辅助**:
只读取游戏公开的本地状态与窗口画面，在游戏进程外生成提醒，不控制游戏、不修改游戏状态。
_Avoid_: 机器人、外挂、注入工具

**游戏交互能力**:
以当前游戏会话、游戏时间或游戏画面为输入，识别对局状态并产生辅助信息的能力。
_Avoid_: 通用应用功能、界面复刻

**最小产品外壳**:
为使用游戏交互能力所必需的设置、覆盖层、提醒播放端口、托盘和诊断入口，不承担通用数据管理或界面复刻职责。
_Avoid_: Keiframe 界面克隆、管理后台

**地图时间表基线**:
从灰机 Wiki 合作任务页面独立整理出的结构化地图事件，作为首版提醒数据的采用值，并保留页面地址和采集日期。
_Avoid_: 官方认证数据、Keiframe 数据副本

**时间表差异检查**:
把地图时间表基线与 Keiframe 固定版本中的时间点和事实字段进行比较，用于发现遗漏、压缩和冲突，不改变灰机 Wiki 的基线地位。
_Avoid_: 从 Keiframe 迁移数据、用 Keiframe 自动覆盖基线

**版本化时间表快照**:
在开发阶段从灰机 Wiki 抓取和审核，并在构建时嵌入 Rust 可执行文件的地图时间表数据；原始抓取和差异报告只保留在仓库中。
_Avoid_: 运行时爬取、外部可变数据文件、静默在线更新

**时间锚点**:
地图事件计时所依附的起点，可以是本局开始，也可以是玩家在当前对局中明确标记的夜晚、目标或其他阶段开始时刻。手动锚点可以重新标记或清除，离开本局后不保留。
_Avoid_: 把阶段相对时间近似成固定开局时间

**运行支持状态**:
说明一条已保存事件在首版中能否自动触发、需要玩家提供阶段或分支上下文，或者因依赖视觉状态而暂不触发。
_Avoid_: 数据已收录等同于功能已启用

**提醒提前量**:
事件预计发生前触发一次播放请求和弹窗提醒的全局用户设置，首版默认值为 30 秒；覆盖层的事件倒计时不受一次性通知限制。
_Avoid_: 到点重复请求播放、同一事件反复通知

**同刻提醒批次**:
把最终触发时刻相同的多个事件合成一次播放请求和一个弹窗，同时保留批次内各事件的独立身份和覆盖层条目。
_Avoid_: 同秒连续请求播放、因提醒窗口重叠而合并不同时间的事件

**提醒播放端口**:
提醒交付层用于请求播放提示的可替换输出接口；调度器不依赖提示音文件、文字转语音或具体音频库，播放失败也不改变事件去重状态。
_Avoid_: Keiframe 音频资产、在调度器中直接播放声音

**覆盖层交互模式**:
覆盖层平时保持置顶和鼠标穿透；玩家通过可配置全局热键临时打开当前地图的控制面板，完成阶段锚点操作后恢复穿透。
_Avoid_: 持续拦截游戏鼠标、模拟或转发游戏输入

**覆盖层提示卡片**:
在游戏覆盖层中短暂显示的事件提醒，不获取焦点，也不写入 Windows 通知中心或系统通知历史。
_Avoid_: 系统通知、抢占游戏焦点

**错过事件**:
程序启动、恢复连接或游戏时间跳跃时已经到点或过去的事件，只记录为本局错过，不补发播放请求和弹窗；仍在未来且已进入提前窗口的事件立即提醒一次。
_Avoid_: 恢复时批量补发过期提醒
